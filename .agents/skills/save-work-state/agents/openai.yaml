interface:
  display_name: "作業状態を保存"
  short_description: "別PCへ引き継げる開発状態を安全にGitHubへ保存する"
  default_prompt: "$save-work-state を使って、現在の作業を別PCへ引き継げる状態で保存してください。"

policy:
  allow_implicit_invocation: true
