interface:
  display_name: "作業状態を再開"
  short_description: "別PCで保存された最新の開発状態を安全に読み込み再開する"
  default_prompt: "$resume-work-state を使って、GitHubに保存された最新状態から作業を再開してください。"

policy:
  allow_implicit_invocation: true
