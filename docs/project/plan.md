# 現在の開発計画

## 現在のマイルストーン（Current Milestone）

### M1: アプリ設計の完成

M0の作業状態保存・再開基盤は、公開GitHubへのpushと別clone検証まで完了した。M1では、後続実装が推測なしで進められる詳細設計と受け入れ基準を確定する。

## 完了済み（Completed）

- 分析対象、対象資産、収集範囲、期間、予想区分、情報境界を確定した。
- ローカルWebアプリの全体構成と主要画面方針を承認した。
- 4つの更新型状態文書、短い`AGENTS.md`、公開除外方針を実装した。
- 保存・再開2スキルと4つの決定的検査スクリプトをテスト先行で実装した。
- 保存・再開スキルをbaselineと適用後で各5反復評価した。
- 一時source、bare remote、second cloneによる統合試験を実装した。
- 全決定的スイート119件と統合試験12件が成功した。
- 承認済み26ファイルだけを公開GitHubの`main`へpushし、live remote SHAを確認した。
- GitHubから別cloneし、公開範囲、Git状態、状態文書、公開安全性、119テストを再検証した。
- 字幕なし39分39秒動画を使うユーザー実施のスモールテストで、722区間の文字起こし、参照声による対象者653区間・聞き手55区間・保留14区間の話者割当、対象者発言だけのCodex分析を完了した。
- スモールテスト結果から、参照声との直接照合、市場全般表現の推定割当、転換点、条件付きレイヤー、3種類の日付、内部データ削除、チェックポイントと段階別進捗の要件を確定した。

## 作業中（In Progress）

- 現在進行中のアプリ実装タスクはない。
- 次回は今回確定した話者割当、分析、指数割当を混同しないデータモデルと変更不能スナップショット形式からM1を開始する。

## 未着手（Not Started）

### M1: アプリ設計の完成

1. 動画の3日付、発話区間、話者割当とその版、本人発言抽出物、Codex分析、対象指数割当、予想、チェックポイントを分離したデータモデルと変更不能スナップショット形式。
2. YouTube収集、重複検出、5～10分分割、参照声照合、対象者・聞き手・保留の判定、手動修正と再分析要求を含む詳細フロー。
3. 本人発言だけを入力するCodex分析prompt、JSON Schema、外部ツール0件を確認する監査ログ、バッチmanifest、再開規則。
4. 市場全般表現の推定割当、直接言及、条件付き、転換点、空欄、判断不能、期間正規化、ヒートマップ採用可否の集約規則。
5. 段階別進捗、条件文、推定割当、聞き手による要約、短い根拠を表示するUIと、例外処理・再試行・監査ログ。
6. 音声削除、全文文字起こしの保存期限と削除機能。保存期限の既定値はM1で確定する。
7. ユーザー報告のCPU処理時間を初期ベースラインにしたテスト戦略と受け入れ基準。ベースラインは性能保証値として扱わない。

### M2以降

1. プロジェクト基盤とローカルAPI。
2. YouTube収集と音声取得。
3. 分割文字起こしと参照声による話者割当。
4. 本人発言抽出とCodex分析。
5. 対象指数への自動割当と4資産比較ヒートマップ。
6. チェックポイント、途中再開、内部データ保持・削除。
7. Windows常駐化、性能・回復性・セキュリティ検証。
