# 現在の開発計画

## 現在のマイルストーン（Current Milestone）

### M0: 複数PC間の作業状態保存・再開基盤

ローカル実装と一時Git remote検証は完了した。ユーザーの公開直前承認を受け、実Git初期化、公開GitHubへの初回push、別clone確認を実行中である。

## 完了済み（Completed）

- 分析対象、対象資産、収集範囲、期間、予想区分、情報境界を確定した。
- ローカルWebアプリの全体構成と主要画面方針を承認した。
- 作業状態保存・再開基盤の既存ファイル、Git状態、公開リスクを調査した。
- 4つの更新型状態文書、短い`AGENTS.md`、公開除外方針を実装した。
- 保存・再開2スキルと4つの決定的検査スクリプトをテスト先行で実装した。
- 保存・再開スキルをbaselineと適用後で各5反復評価した。
- 一時source、bare remote、second cloneによる統合試験を実装した。
- 全決定的スイート119件と統合試験12件が成功した。
- 初回公開候補26ファイルと、公開しない`.stitch`・実データ範囲を確定した。

## 作業中（In Progress）

1. 公開候補26ファイルだけを初回commitする。
2. 公開GitHubを作成し、`main`へpushしてlive remote SHAを確認する。
3. 状態文書を公開済みの事実へ更新して再度保存する。
4. 別cloneで再開手順を検証する。

## 未着手（Not Started）

### M0の外部公開作業

1. 最終承認後に対象ディレクトリをGit初期化し、branchを`main`にする。
2. 26個の公開候補だけを明示的にstageし、staged公開安全検査とdiff確認を行う。
3. 最初のcommitを作成する。
4. 公開GitHubリポジトリを作成または接続してpushする。
5. live remote SHAとlocal HEADの一致を確認する。
6. 別の一時cloneから再開手順を検証し、M0を完了する。

### M1: アプリ設計の完成

1. データモデルと変更不能スナップショット形式。
2. YouTube収集、重複検出、音声処理、話者確認の詳細フロー。
3. Codex分析prompt、JSON Schema、バッチmanifest、集約規則。
4. UI画面一覧、例外処理、再試行、監査ログ。
5. テスト戦略と受け入れ基準。

### M2以降

1. プロジェクト基盤とローカルAPI。
2. YouTube収集。
3. 文字起こし・話者処理。
4. Codex分析。
5. 4資産比較ヒートマップ。
6. Windows常駐化、性能・回復性・セキュリティ検証。
